// Default options for the Component library / Icon library selectors
// (LibraryField.jsx). "+ Add new" always lets a project pick something
// outside this list; these are just the shortcuts most projects want.
export const COMPONENT_LIBRARY_PRESETS = ["shadcn", "boardui"];

// TODO: fill in with the real default set.
export const ICON_LIBRARY_PRESETS = [];
